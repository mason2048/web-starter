# 安全策略

## 支持范围

安全修复优先覆盖默认分支的最新代码。历史版本是否回补将根据漏洞影响和维护成本单独评估。

## 报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下提交安全报告，不要创建公开 Issue，也不要在公开讨论中粘贴令牌、密码、私钥、生产地址或可复现的攻击数据。

报告中建议包含：

- 受影响版本或提交；
- 漏洞类型和影响范围；
- 最小复现步骤；
- 已知缓解方式；
- 便于关联排查的非敏感日志或 Trace ID。

维护者确认问题前，请不要公开披露细节。修复完成后，维护者会根据影响决定是否发布安全公告。

## 项目安全边界

- 本项目面向内部 Web 管理系统，不默认适用于公网 SaaS。
- 公网 MCP 入口必须使用 HTTPS 和 OAuth 2.1。
- PAT 与服务账号长期令牌只适用于受控私有入口。
- 禁止提交真实凭据、生产主机名以及任意 SQL、Shell、文件系统或动态代码执行工具。

部署方仍需独立完成密钥管理、网络边界、备份恢复、依赖漏洞和生产配置审计。
